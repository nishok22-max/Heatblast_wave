# --- core: Day 1 critical path ---
numpy>=1.26
pandas>=2.1
requests>=2.31
PyYAML>=6.0
pytest>=8.0

# --- thermal physics ---
# pythermalcomfort: UTCI, PHS (ISO 7933), two-node Gagge
pythermalcomfort>=2.8
# thermofeel: ECMWF's own index library, used for cross-validation
thermofeel>=2.1

# --- spatial ---
# h3 alone gives cell tessellation + boundary polygons.
# Deliberately NOT using geopandas/shapely: unnecessary here and painful on Windows.
h3>=4.0

# --- satellite (Day 1 PM; optional if WUDAPT LCZ fallback is used) ---
earthengine-api>=0.1.390

# --- api backend ---
fastapi>=0.110.0
uvicorn>=0.28.0
